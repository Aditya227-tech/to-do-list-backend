# App-specific initialization
# This file ensures that Python treats the todo_app directory as a package

# Optional: You can use this to perform app-specific configurations
# For example, importing signals or performing initial setup

default_app_config = 'todo_app.apps.TodoAppConfig'